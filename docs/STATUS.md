# Status

The **first file every session reads**. It is the only place live status is tracked. Definitions (what "done" means and what each unit tests) are in the [milestone doc](superpowers/plans/2026-09-25-telephony-bridge-milestones.md). The partner updates it **inside each unit's PR, before the PR merges** (D-017). When no unit is in flight, updates go in a status-only PR (D-018). See [CLAUDE.md](../CLAUDE.md) §3 and §5 step 8.

Last updated: 2026-09-27 (M6 implementation plan revised to rev 2 after first Opus review round found deploy-breaking gaps; units U14a–d, U-CFG, U15, U16 added)

---

## §1 Unit dashboard

Status values: `NEXT` (the one unit the next session works on; its branch may exist only if its PR is open) · `CLOSED` (PR merged and branch deleted) · `QUEUED` · `BLOCKED`.

This table is updated **inside each unit's own PR** (D-017), so what `main` shows is always current.

| Unit | Plan task | Milestone | Status | Branch | PR | Tests (pass/planned) | Opus review | cso |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| U00 | Governance, spec, plan | — | CLOSED | `docs/governance-and-bridge-plan` | [#1](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/1) | 0/0 | 2 rounds; findings fixed | n/a (docs only) |
| U01 | Task 0 — foundation | M0 | CLOSED | `feat/tb-t00-foundation` | [#2](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/2) | 3/3 | 2 rounds; findings fixed | n/a (import/config/test scaffolding, no app security surface) |
| U02 | Task 1 — log mask | M1 | CLOSED | `feat/tb-t01-log-mask` | [#3](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/3) | 10/10 | 1 round; clean, low-severity notes only | n/a (pure string logic, no security surface) |
| U03 | Task 2 — routing | M1 | CLOSED | `feat/tb-t02-routing` | [#4](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/4) | 38/20 (grew via review) | 2 rounds; real bugs fixed | n/a (pure parsing/validation, no security surface) |
| U04 | Task 3 — bridge config | M1 | CLOSED | `feat/tb-t03-bridge-config` | [#5](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/5) | 92/34 (grew via review) | 4 rounds; real bugs fixed each round | n/a (validation logic, no network/auth surface of its own — but see D-023) |
| U05 | Task 4 — server wiring | M1 | CLOSED | `feat/tb-t04-server-wiring` | [#6](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/6) | 149/5 (grew via review) | 4 rounds; real bugs fixed (D-024) | 2 rounds; 1 real finding fixed (D-024) |
| U06 | Task 5 — expiry hooks | M2 | CLOSED | `feat/tb-t05-expiry-hooks` | [#7](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/7) | 153/4 (149 pre-existing + 4 new) | 1 round; clean, low-severity notes only | 1 round; clean, 1 medium latent finding logged as Q-008 |
| U07 | Task 6 — Voice Live agent mode | M2 | CLOSED | `feat/tb-t06-voicelive-agent-mode` | [#8](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/8) | 165/11 (grew via review) | 2 rounds; round 1 found a real blocking bug (B1) plus 3 recommended fixes, all fixed; round 2 clean | 2 rounds; round 1 found the same B1 bug independently, confirmed D-004 intact both rounds |
| U08 | Task 7 — call session | M3 | CLOSED | `feat/tb-t07-call-session` | [#9](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/9) | 35/31 (200 total; grew via review) | 2 rounds; round 1 found 3 real bugs (#A/#B/#D), all fixed; round 2 clean, ready to merge | 2 rounds; both clean |
| U09 | Task 7.5 — web-client/telephony mutual exclusion (D-028) | M3 | CLOSED | `feat/tb-t075-webclient-guard` | [#12](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/12) | 205/5 (200 pre-existing + 5 new) | 1 round; found a real test flake (Bandwidth detect-key mismatch) and a decision-citation error, both fixed; clean thereafter | 1 round; PASS, no blocking findings |
| U10 | Task 8 — bridge calls | M4 | NEXT | `feat/tb-t08-bridge-calls` | — | 0/12 | — | — |
| U11 | Task 9 — ACS media handler | M4 | QUEUED | `feat/tb-t09-acs-media-handler` | — | 0/9 | — | — |
| U12 | Task 10 — ACS routes | M4 | QUEUED | `feat/tb-t10-acs-routes` | — | 0/17 | — | — |
| U13 | Task 11 — docs | M5 | QUEUED | `feat/tb-t11-docs` | — | full suite | — | — |
| U14a | M6 — existing AI resource (cross-subscription), ACS TTS wiring, Container App scale fix | M6 | BLOCKED (Q-012, Q-014, Q-016, Q-017 — do not cut this branch until all four are answered) | `feat/tb-m6-u14a-existing-ai-resource` | — | n/a (infra verification, see plan) | — | — |
| U14b | M6 — resequence to system-assigned identity (circular-dependency fix); **Opus** per D-014 | M6 | BLOCKED (Q-015 — founder sign-off on the identity approach needed first) | `feat/tb-m6-u14b-system-identity` | — | n/a (infra verification, see plan) | — | — |
| U14c | M6 — Key Vault media token, container sizing, Foundry User role GUID verification | M6 | QUEUED | `feat/tb-m6-u14c-secrets-and-sizing` | — | n/a (infra verification, see plan) | — | — |
| U14d | M6 — resource-group/existing-resource naming resolution (may fold into U14a) | M6 | QUEUED | `feat/tb-m6-u14d-resource-naming` | — | n/a | — | — |
| U-CFG | M6 — hook `config_validator.py`'s credential check for system-assigned identity (no API-key workaround) | M6 | QUEUED | `feat/tb-m6-ucfg-identity-credential-check` | — | 0/new (pytest, see plan) | — | — |
| U15 | M6 — `azd up`, verify, VoIP-only smoke test (requires U10–U13 merged too) | M6 | QUEUED | `feat/tb-m6-u15-deploy` | — | n/a (infra verification, see plan) | — | — |
| U16 | M6 — Event Grid number-based advanced filter | M6 | BLOCKED (Q-002) | not yet cut | — | n/a | — | — |
| M7 | Live acceptance tests | M7 | BLOCKED | — | — | 0/9 live | — | — |

**Milestone roll-up:**

| Milestone | Status |
| --- | --- |
| M0 | not started |
| M1 | complete (U02–U05 all CLOSED) |
| M2 | complete (U06–U07 all CLOSED) |
| M3 | complete (U08 and U09 both CLOSED) |
| M4 | not started (U10–U12) |
| M5 | not started (U13) |
| M6 | unblocked ahead of Q-002 (D-029); implementation plan revised to rev 2 (U14a–d, U-CFG, U15, U16) after a first Opus review round found real deploy-breaking gaps — see [M6 plan](superpowers/plans/2026-09-27-m6-deploy-plan.md); M4/M5 code work continues in parallel; U10 (M4) remains NEXT for the next code session; U15 additionally requires U10–U13 CLOSED before it can run |
| M7–M8 | blocked (needs a deployed M6 and the ACS number for live calls) |

**Unit tests passing overall:** 205 / ~255 (running total, `pytest --collect-only`). U01: 1 planned → 3; U02: 7 planned → 10; U03: 20 planned → 38; U04: 34 planned → 92 (four review rounds on the startup validator — see §2); U05: 5 planned → 5 (+1 regression test landed in U04's `test_bridge_config.py` for a `cso` finding, so the file-level total this unit touched is 149); U06: 4 planned → 4; U07: 11 planned → 12 (one regression test added for the interim_response deep-thaw bug found in review — see D-026); U08: 31 planned → 35 (4 regression tests added for the hang-up-retry bugs found in review — see D-027); U09: not in the original plan (inserted 2026-09-27 for D-028) → 5 new tests, one added during review (see §2). Later units' "planned" counts in this table are the plan's original estimates and will likely grow the same way once reviewed.

---

## §2 Audit log (append-only, newest last)

| Date | Unit | Event | PR | Notes |
| --- | --- | --- | --- | --- |
| 2026-09-24 | — | Repo pulled from GitHub | — | The local clone was empty. Fetched `origin/main` (HANDOFF.md, spec, README). |
| 2026-09-25 | U00 | Design spec rev 1 → rev 5 | — | Four Opus design-review rounds. Accepted by the founder (D-008). |
| 2026-09-25 | U00 | Implementation plan written | — | 12 TDD tasks; SDK APIs checked against azure-ai-voicelive 1.3.0 and ACS Call Automation 1.6.0. |
| 2026-09-25 | U00 | Governance set up | — | Partner/builder agents, CLAUDE.md session protocol, STATUS.md, DECISIONS.md, one unit per session/branch/PR (D-009 to D-015). |
| 2026-09-25 | U00 | Opus review of governance docs, 2 rounds | — | 20 findings fixed. Added D-016 (merge commits, upstream out of review scope), D-017 (status inside the unit PR) and D-018 (status-only PRs, branch-resume rules). |
| 2026-09-25 | U00 | Q-001 answered; PR #1 opened and merged | [#1](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/1) | Founder: builder merges its own PRs (D-019). Branch `docs/governance-and-bridge-plan` deleted after merge. |
| 2026-09-25 | U01 | Upstream accelerator imported | — | `git merge upstream/main --allow-unrelated-histories`, upstream SHA `a4f40bc`. README conflict resolved (ours kept; theirs moved to `docs/ACCELERATOR_README.md`). |
| 2026-09-25 | U01 | SDK pins + pytest harness added, 2 Opus review rounds | [#2](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/2) | `azure-ai-voicelive>=1.3.0,<2`, `azure-communication-callautomation>=1.6.0,<2`. Review found a real bug (pytest's prepend import mode loaded the wrong `server` module — fixed with `--import-mode=importlib`) plus README-link and log-capture fixes. Smoke test grew from 1 to 3 planned tests to actually exercise `load_server`. Second round: 3 low-severity nits (env isolation, conftest double-import risk, caplog docstring), all fixed. |
| 2026-09-25 | U02 | `mask_number()` added, 1 Opus review round | [#3](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/3) | No real defects found. Added D-021 (the masking rule's correct decision entry, correcting U02's own commit message which had miscited D-006). Added boundary (4/5-digit) and int-input tests per the review's low-severity notes; skipped two theoretical-only notes (non-phone rawId cosmetic mislabeling, float input). Test tally grew from 7 planned to 10. |
| 2026-09-25 | U03 | `routing.py` added, 2 Opus review rounds | [#4](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/4) | Round 1 found real bugs: the NANP regex accepted dropped-digit keys as valid (silent route miss with no startup error — see D-022), `normalize_number` could turn garbage/extensions/GUID rawIds into plausible-looking numbers, regexes were Unicode-digit and trailing-newline permissive, and event extraction raised on non-dict identifiers. All fixed. Round 2 confirmed the fixes hold and found only low-severity nits (event-payload-level guard, non-string phoneNumber.value, exact boundary test); fixed the cheap ones, left one intentional (non-ASCII whitespace fails loud, matching spec). Test tally grew from 20 planned to 38. |
| 2026-09-25 | U04 | `bridge_config.py` added, 4 Opus review rounds | [#5](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/5) | This is the startup gate D-022 was written to protect, so it got extra scrutiny (all reviews dispatched as Opus subagents from a Sonnet session, per CLAUDE.md §5). Round 1 found fail-quiet gaps: non-ASCII/leading-zero version strings, whitespace-padded secrets passing the required-value check, `inf`/`nan` timeouts, D-003 precedence broken by whitespace-only values, uncaught exceptions escaping past `BridgeConfigError`. Round 2 found round 1's own duplicate-key masking fix had *regressed* D-021 (a digit-bearing-but-not-strictly-E.164 duplicate key leaked unmasked) plus an `OverflowError` on huge integers. Round 3 found the masking fix still missed non-consecutive-digit keys, a `_deep_freeze` recursion boundary sitting outside the try block, `1e999`-style float overflow bypassing the NaN/Infinity guard, and an ungrounded shared numeric ceiling. Round 4 found round 3's own exception-catch narrowing had reintroduced the exact uncaught-exception bug round 1 fixed (Python's 4300-digit integer-string-conversion limit raises a plain `ValueError`). All fixed; round 4 confirmed the unit "ready to ship" once that one fix landed. See D-023. Test tally grew from 34 planned to 92. |
| 2026-09-25 | U05 | `server.py` wired to `load_bridge_config()`, web client guarded, 4 Opus + 2 cso rounds | [#6](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/6) | All reviews dispatched as Opus subagents from a Sonnet session, per CLAUDE.md §5. Opus round 1 found a real D-007 gap: unregistering `/web/ws` and `/` left Quart's default static route still serving the web client's HTML/JS regardless of `ENABLE_WEB_CLIENT` — fixed by making `static_folder` itself conditional. Round 2 found the new env vars this unit made load-bearing were missing from `server/.env.sample` (a DoD item) — fixed. Round 3 found the newly-added `.env.sample` placeholders for `MEDIA_WS_TOKEN`/`AGENT_ROUTING_JSON` were left uncommented and long/valid enough to silently pass startup validation if copied as-is — fixed (commented out, matching every other optional value in the file). Round 4: clean. `cso` round 1 found `BridgeConfig`'s auto-repr would leak `media_ws_token` now that the object is reachable via `app.config["BRIDGE"]` — fixed with `field(repr=False)` plus a regression test in `test_bridge_config.py`. `cso` round 2: clean. See D-024. Five further items (acs_active detection method, unvalidated `MAX_CONCURRENT_CALLS`/`CALL_IDLE_TIMEOUT`, an upstream error-message mismatch, no code guard against `ENABLE_WEB_CLIENT`+ACS coexisting, and upstream's own `0.0.0.0` bind) were deferred rather than fixed in-unit — see D-025 and Q-007. Test tally: 5 planned → 5, plus 1 regression test landed in U04's test file for the `cso` finding. M1 is now complete (U02–U05 all closed). |
| 2026-09-26 | U06 | `is_expired()` now returns a reason string; `on_call_cap`/`on_idle` hooks added, 1 Opus + 1 cso round | [#7](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/7) | Both reviews dispatched as Opus subagents from a Sonnet session, per CLAUDE.md §5, scoped to `main...HEAD`. Opus code-review: clean, only 4 low-severity notes (no test for a hook that raises, no test pinning duration-before-idle precedence, no `-> None` annotation on the base hooks, calling unbound methods with `object()` in the no-op test) — none blocking, left as-is since Task 5's scope is exactly what the plan specifies. `cso`: no exploitable issue in this diff (today's hooks are no-ops; log content unchanged; no auth/secret/network surface touched), but flagged a latent medium finding: `run_call_loop` awaits `on_call_cap()`/`on_idle()` with no timeout before the `finally` cancels the Voice Live task and before the caller releases the call slot, so once a future telephony subclass gives these hooks real I/O (a hangup call, a goodbye-message send), a hang there would leak the call slot and keep billing the Voice Live session — an operational DoS risk. Not fixed in this unit because Task 5's hooks are no-ops with no I/O; logged as Q-008 for whichever unit adds real hook bodies. Test tally: 4 planned → 4 (149 pre-existing + 4 = 153 total). M2 is now in progress. |
| 2026-09-26 | U07 | `VoiceLiveMediaHandler` connects in pinned agent mode when a route is set; `AgentRoute` version now self-validates, 2 Opus + 2 cso rounds | [#8](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/8) | Both reviews dispatched as Opus subagents from a Sonnet session, per CLAUDE.md §5, scoped to `main...HEAD`. Given this unit touches D-004 and an Entra-ID auth boundary, it got the same extra scrutiny as U04/U05. Round 1: Opus code-review and `cso` independently converged on the same blocking bug — `interim_response` was shallow-copied with `dict(...)`, but `BridgeConfig._deep_freeze()` (D-023) freezes nested dicts/lists too, so a nested object inside `INTERIM_RESPONSE_JSON` stayed a `MappingProxyType` and `json.dumps()` on the agent-mode session payload would raise `TypeError` for every such call — a connection-breaking bug, not cosmetic. Round 1 also flagged: buffered TTS audio kept playing after `stop_forwarding_agent_audio()`; `_voicelive_connected` set true before `session.update`/`response.create` could fail; an unguarded `on_voicelive_ended()` call in the receiver's `finally`; and no independent pin-format check on `AgentRoute.version` (D-004 defense in depth). Both reviewers explicitly confirmed no D-004 violation, correct credential routing, and no secrets logged. All four items fixed (see D-026); a regression test for the interim_response bug was added, built via the real `_deep_freeze()`. Round 2 (re-review of the fix commit): both clean, no new issues, D-004 boundary confirmed still intact, the fix verified against the real SDK. An upstream finding (`config_validator.py` accepts an API key as sole credential in agent mode, in tension with D-004's Entra-only intent) was logged as Q-009 rather than fixed (D-016: upstream out of scope). Test tally: 11 planned → 12 (153 pre-existing + 12 = 165 total). M2 is now complete (U06–U07 both closed). |
| 2026-09-27 | — | Founder answered Q-002 through Q-007 (status-only PR, no unit in flight, D-018) | — | Q-003 (region: East US 2), Q-004 (system-assigned identity), Q-005 (verify JWT at M6 as planned), Q-006 (founder handles `az login`/`azd auth login` before M6) all answered, none blocking. Q-002 (ACS number) remains open — Azure support ticket 2609250040002301, no ETA, not blocking build work. Q-007 answered as a new decision (D-028): the bridge must refuse to start, not just warn, if the web debug client and a telephony provider are both active. Fix folded into U09 rather than a standalone unit, since U09 already touches `server.py`'s startup/routing wiring. |
| 2026-09-27 | U08 | `signing.py` (HMAC sign/verify) and `call_session.py` (`CallSession`, `CallSessionRegistry`, `SessionSettings`) added — the single idempotent path every call-ending event goes through, 2 Opus + 2 cso rounds | [#9](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/9) | Both reviews dispatched as Opus subagents from a Sonnet session, per CLAUDE.md §5, scoped to `main...HEAD`. This is the unit D-005/D-008's four design-spec review rounds were written to protect, so it got the same extra scrutiny as U04/U05/U07. `cso` round 1: clean, PASS, no blocking findings (3 low-severity notes correctly deferred to the U09–U11 wiring units, since neither file is wired into a route yet). Opus code-review round 1 found three real "the call stays up" bugs: (#A) a late `on_play_done()` (or a `play_media` failure) could cancel the safety timer while its spawned `_safe_hang_up()` was already awaiting the ACS HTTP call — `CancelledError` isn't caught by `except Exception`, so the hang-up request was silently dropped and the follow-up retry no-op'd because `_hung_up` was already `True`; (#D) `request_end()` firing before `set_answered()` supplies a `call_connection_id` (e.g. a route-miss `CallConnected` racing ahead of `answer_call()`'s response) made `_safe_hang_up()` silently no-op with nothing to ever retry it, leaving the caller connected in silence indefinitely; (#B) any `HttpResponseError` on hang-up, including transient 5xx/429s, was treated the same as a terminal "already gone" error, permanently setting `_hung_up` and blocking any retry. Fixed: `_cancel_safety_timer()` now only cancels the timer while it's still sleeping (guarded by `_hung_up`), plus `asyncio.shield()` around the hang_up call as defense in depth (#A); a new `_hangup_pending_answer` flag lets `set_answered()` finish a hang-up that `_safe_hang_up()` had to defer for lack of a connection id (#D); only a narrow `{404, 410}` status set is now treated as terminal, everything else clears `_hung_up` so a later call can retry, and `sweep()` now also calls the new `force_hang_up()` on every stale session it removes (#B). 4 regression tests added. Round 2 (re-review of the fix commit): both reviewers say ready to merge, no new issues; both independently reconfirmed request_end's idempotency, that no double hang-up is possible, that only one Voice Live close task can ever be spawned, and that no session is ever permanently stuck in the registry (sweep is the backstop). Opus code-review round 2 flagged one medium, non-blocking follow-up — the #B fix stops treating transient failures as *permanently* terminal but nothing retries them *promptly* (the only remaining retry today is `sweep()`'s `max_age`-gated `force_hang_up`) — logged as Q-010 rather than fixed in this unit, per the founder/partner's call to avoid a third fix-and-re-review cycle on an already twice-reviewed unit; also a pre-existing (non-regression) gap noted, that the `hang_up` HTTP call itself has no timeout, folded into the same Q-010. See D-027. Test tally: 31 planned → 35 (165 pre-existing + 35 = 200 total). M3 is now complete (U08 is its only unit). |
| 2026-09-27 | U09 | `server.py` refuses to start when `ENABLE_WEB_CLIENT=true` and a telephony provider is active, 1 Opus + 1 cso round | [#12](https://github.com/AmRaghuAkula/VoiceAgentPilot/pull/12) | Both reviews dispatched as Opus subagents from a Sonnet session, per CLAUDE.md §5, scoped to `main...HEAD`. Implements D-028 (answers Q-007): the guard sits right after `_telephony_client = detect_provider()` in `server.py`, logs an error and calls `sys.exit(1)` (matching the existing `BridgeConfigError` pattern) when the web client is on and a telephony provider is detected as configured. `cso`: PASS, no blocking findings — confirmed the guard is unconditional and unbypassable (runs at import time before any route registration), no secrets logged, and confirmed `detect_provider()`'s "configured" signal (any detect-key env var truthy) is safe for this gate since every provider's `detect_key` is also one of its `required_config` keys, and a whitespace-only credential counting as "configured" errs toward failing closed — the correct direction for a security gate. Opus code-review: ready to merge, no blocking findings, but caught a real test flake (N1) and a decision-citation error (N4), both fixed before merge: `tests/helpers.py`'s `BRIDGE_ENV_KEYS` cleared `BANDWIDTH_ACCOUNT_ID` but Bandwidth's actual `detect_key` is `BANDWIDTH_CLIENT_ID`, so a machine with that env var set would have made two tests flake with an unexpected `SystemExit` — fixed by adding the correct key; and the D-028 entry plus the new code comment both mis-cited D-005 (the unrelated call-ending decision) instead of D-007/D-024/D-025 as the superseded warn-only behavior — corrected in both places. Two low-cost suggestions were also applied: a stronger log assertion (matching "Refusing to start"/"D-028" specifically) and a second refuse-to-start test using Twilio's detect key instead of ACS, so the guard can't be quietly narrowed to ACS-only by a future refactor. A second full review round was judged unnecessary given the narrow, low-risk scope of both fixes. Test tally: 5 new (200 pre-existing + 5 = 205 total). M3 is now complete (U08 and U09 both closed). |
| 2026-09-27 | — | Founder unblocked M6 ahead of Q-002 (status-only PR, no unit in flight, D-018) | — | Founder is completing `az login`/`azd auth login` now and directed the partner to proceed with M6 (Container App, Key Vault, Event Grid wiring against `hireastra-resource`/`rg-hireastra-voice-pilot`) without waiting for the ACS phone number — the Event Grid subscription for `IncomingCall` can be created now and will simply receive nothing until a number is assigned. Recorded as D-029. Q-002 itself stays OPEN; it no longer blocks M6, only live calls (M7/M8). New Q-011 raised: whether an ACS VoIP-only call can validate the deployed pipeline end-to-end ahead of the real number — must be verified against the actual `IncomingCall` event shape once M6 is deployed, not assumed. Per CLAUDE.md's stop-and-ask rule (M6/`infra`/`azd` work needs the plan the milestones doc says is written once M6 unblocks), the partner writes a full M6 implementation plan (units, tests, DoD) before any `azd`/Bicep work starts — see the milestones doc's M6 section for that plan once written. |
| 2026-09-27 | — | M6 implementation plan written, rev 1 (partner-only planning session, `docs/m6-deploy-plan` branch, not yet merged) | not yet opened | Read D-029 in full plus D-001–D-028 for identity/security/naming constraints; verified the repo's actual `infra/`, `hooks/` and `azure.yaml` against the spec rather than assuming the accelerator's Bicep matches — found three real gaps: the AI Services resource, the identity type, and the Event Grid subscription mechanism all diverged from the spec's assumptions. Wrote a first draft of the plan as three units (U14, U15, U16) and a Q-011 (VoIP-only validation) analysis. Dispatched an Opus review of the draft before opening a PR, per this unit's own instruction to get a design review before merging a plan that governs real Azure spend. |
| 2026-09-27 | — | M6 implementation plan — Opus review round 1: "needs fixes before merge" | not yet opened | The Opus review confirmed most of rev 1's factual spot-checks but found several real, deploy-breaking gaps rev 1 missed: (C1) a circular-dependency problem in the system-assigned identity switch that `main.bicep`'s own comment explicitly warns about ("to avoid circular dependency") — rev 1 treated the switch as a one-line type change; (C2) unmodified upstream `config_validator.py` would crash-loop the container under a system-assigned identity with no API key, and the obvious fix (setting an API key) would reopen Q-009 and violate D-004 — rev 1 had no unit for this at all; (C3) `ACS_COGNITIVE_SERVICES_ENDPOINT`/the ACS-to-AI-resource text-to-speech link was completely unwired in Bicep despite `bridge_config.py` already requiring it at startup, and its absence would also silently break the Q-011 smoke test's own pass signal; (C4) no placeholder value was specified for `AGENT_ROUTING_JSON`, which cannot be empty at deploy time; (C5/C6) the cross-subscription topology (D-001) and the possibility that `rg-hireastra-voice-pilot`/the ACS resource already exist were both absent from the plan; (C7) U15's prerequisites only required U14, not the U10–U13 code units the Q-011 smoke test's actual code path depends on; (C8) the Application Insights gap needed a Python code change, not just a Bicep fix, contradicting rev 1's framing; (C9) Key Vault's RBAC-only authorization meant the planned `az keyvault secret list` verification needed a data-plane role subscription Owner doesn't grant; (C10) `azd`/`pwsh`/the `az` `communication` extension's presence were unconfirmed; (C11) a cost estimate suggested the accelerator's default Container App/ACR sizing could exceed the spec's own $50/month budget suggestion before any real usage. Also found several inaccurate factual claims (preprovision.ps1's interactive prompt was described backwards from postdeploy.ps1's, the Event Grid advanced-filter capability was wrongly framed as possibly nonexistent, a `routing.py` code quote didn't match the real function, a PowerShell secret-generation API doesn't exist under `pwsh`, and the planned `az deployment sub what-if` command against `main.parameters.json` wouldn't resolve `azd`-only parameter tokens) and several process gaps (U14 was too large for one PR/one review; Q-011's test procedure under-specified the number of ACS identities needed, accepted `PlayFailed` as a false-positive pass, and included an unverified, spec-§7-violating outbound-call alternative; D-002's upstream-merge-cleanliness tension with editing several pure-upstream Bicep files was never surfaced as a question). |
| 2026-09-27 | — | M6 implementation plan revised to rev 2 (partner-only planning session, `docs/m6-deploy-plan` branch, not yet merged) | not yet opened | Verified every review finding against the actual files before revising (read `config_validator.py` in full and confirmed via `git diff upstream/main` that it is unmodified upstream code; confirmed `main.bicep`'s circular-dependency comment at line 80; confirmed `appInsightsConnectionString` is declared in `containerapp.bicep` but never passed from `main.bicep` and that `server/pyproject.toml` has no Application Insights/OpenTelemetry dependency; confirmed the `az` `communication` extension is not installed and fails to install non-interactively in this environment). Rewrote the plan doc, splitting the original U14 into **U14a** (existing AI resource cross-subscription + ACS text-to-speech wiring + Container App scale fix), **U14b** (system-assigned identity resequencing — the circular-dependency fix, flagged for **Opus** per D-014 given the architectural subtlety), **U14c** (Key Vault media token via a cryptographically secure generator + container right-sizing + Foundry User role GUID verification), and **U14d** (resource-group/existing-resource naming, possibly foldable into U14a); added a new **U-CFG** unit (a small `server/` code unit, with its own pytest coverage, hooking `config_validator.py`'s credential check per D-002/D-016 without editing the upstream file and with an explicit hard constraint against the API-key workaround); corrected **U15**'s prerequisites to require U10–U13 also `CLOSED`, not just U14a–d; corrected the Q-011 smoke-test procedure (two ACS identities not one, a `PlayCompleted`-specific pass criterion, dropped the unverified outbound-call alternative to a flagged footnote requiring future verification and founder sign-off, moved the ACS connection string off the command line, corrected who runs the test); restored the M6 DoD's 7th bullet (the ACS-to-AI-resource TTS link) that had been silently dropped; corrected Q-013 (the Event Grid advanced-filter capability is real, the accelerator just doesn't use it) and Q-014 (needs a code change, not just Bicep, and its owner is founder/partner, not the builder); added **Q-015** (asking the founder to explicitly reconsider Q-004's system-assigned answer now that C1's cost is visible), **Q-016** (whether Cowork already created the resource group/ACS resource, gating U14a), and **Q-017** (explicit founder sign-off for U14a–d's divergence from pure upstream Bicep, per D-002's tension). Updated the milestones doc's M6 units table and DoD to match. Re-dispatched the Opus review on this revised plan (see below). |
| 2026-09-27 | — | M6 implementation plan — Opus review round 2: "needs one more fix pass," smaller finding list | not yet opened | Round 2 confirmed almost everything from round 1 was fixed correctly, and every citation it independently checked against the actual files was accurate. Two real technical errors remained: (1) C3's text-to-speech role assignment was still wrong — the reviewer checked Microsoft's own "Connect Azure Communication Services to Azure AI Foundry" documentation and found the Cognitive Services User role must go to **the ACS resource's own system-assigned identity**, not the Container App's identity as rev 2 had it; getting this wrong would make the Q-011 smoke test fail with `PlayFailed` rather than pass with `PlayCompleted`, i.e. exactly the failure mode the test exists to catch, caused by this plan's own Bicep; (2) C1's circular-dependency fix was still incomplete — Container App Key Vault secret references (not just the registry pull) are also resolved at revision-creation time, so rev 2's "system-assigned for Key Vault access" option did not actually work, and needed either keeping Key Vault access on a user-assigned identity too, or an explicit second-deploy-pass design. Smaller findings: U-CFG's "no prerequisite" claim was wrong (it depends on whatever new signal U14b introduces for detecting system-assigned mode) and needed a hard constraint plus regression test against a real sentinel-leak risk in `voicelive_media_handler.py` line 143; Q-017's file list was missing `modules/containerregistry.bicep` (where `AcrPull` is actually granted), `modules/acs.bicep`, `modules/keyvault.bicep`, `hooks/preprovision.ps1`, and `main.parameters.json`; STATUS.md still had literal placeholder text and an inaccurate "merged" claim; U14a/U14b's blocker labels didn't show the Q-numbers actually blocking them; C5's cross-subscription role-assignment scope needed flagging as unverified syntax rather than asserted, plus an explicit `az account set` step given `acs.postdeploy.ps1`'s ambient-subscription dependency and `azure.yaml`'s `continueOnError: true` on that hook; the Q-011 test procedure's connection-string handling and "who runs it" attribution needed further correction; and two garbled sentences needed removing. The reviewer's own assessment: this was a much smaller, bounded fix list, and a light third review of just the changed sections would be enough afterward — not a full round-trip. |
| 2026-09-27 | — | M6 implementation plan revised to rev 3 (partner-only planning session, `docs/m6-deploy-plan` branch, not yet merged) | not yet opened | Verified both remaining technical findings against the actual files before fixing (read `infra/modules/acs.bicep` in full, confirmed it has no identity block at all today; read `infra/modules/containerregistry.bicep` in full, confirmed `AcrPull` is granted there — a separate file from `roleassignments.bicep` — to the user-assigned identity's principal id specifically; read `server/app/handler/voicelive_media_handler.py` lines 73/143/156-157, confirmed `AZURE_USER_ASSIGNED_IDENTITY_CLIENT_ID` is read with no validation and passed straight into `DefaultAzureCredential`; confirmed `keyvault.bicep`'s `enablePurgeProtection: true` at line 34; confirmed `azure.yaml`'s `continueOnError: true` on the `postdeploy` hook at line 33). Fixed C3 (the ACS-TTS role now targets the ACS resource's own new system-assigned identity, with `hireastra-resource`'s multi-service/`AIServices` kind flagged for portal confirmation, U14a's step 2/DoD and U15's verification step both corrected to match) and C1 (both U14b options now account for Key Vault secret resolution, not just the registry pull; `containerregistry.bicep` named explicitly; a new note on `enablePurgeProtection` forcing fresh environment names and explicit teardown for scratch-environment testing). Fixed U-CFG's prerequisite claim and added the sentinel-leak hard constraint and regression test. Expanded Q-017's file list to the full, verified set. Fixed STATUS.md's placeholder text and false "merged" claims (this row and the two above), and U14a/U14b's blocker labels to show their actual gating Q-numbers. Flagged C5's cross-subscription role-assignment Bicep syntax as unverified (not asserted as fact) and added an explicit `az account set` step to U15 plus a module-split note to U14a. Corrected the Q-011 test procedure's connection-string sourcing (from Key Vault, needing the C9 data-plane role) and "who runs it" attribution (builder/Cowork stages the test; the founder places/confirms the call, matching HANDOFF.md's actual text rather than rewriting it) and cross-referenced Q-013/U16's future interaction with it. Made `MEDIA_WS_TOKEN` generation explicitly idempotent. Removed two garbled sentences. Dispatched a third, focused Opus review round covering just the changed sections (C3's role assignment, C1's Key Vault handling, the STATUS.md fixes), per the founder's go-ahead to merge directly if that focused review comes back clean. |

---

## §3 Open questions

Status values: `OPEN` · `ANSWERED` (record the answer and link a D-NNN if it became a decision).

| ID | Question | Owner | Blocks | Status |
| --- | --- | --- | --- | --- |
| Q-001 | Who merges PRs? | Founder | Closing every unit, U00 onwards | ANSWERED: the builder merges its own PR once Opus review, `cso` and tests all pass, then deletes the branch (see D-019). |
| Q-002 | Buy the ACS test number (Option B subscription). | Founder + Cowork | Live calls (M7/M8), no longer blocks M6 itself (D-029) | OPEN — Azure support ticket 2609250040002301 open, no ETA. M6 (deploy) proceeds without it; the Event Grid subscription is created now and will simply receive nothing until a number is assigned. |
| Q-003 | Confirm the region of `hireastra-resource`. | Cowork | M6 | ANSWERED: East US 2, matching `rg-hireastra-voice-pilot` (already established when that resource group was created; no further check needed). |
| Q-004 | Bridge identity at deploy: keep the accelerator's user-assigned identity, or switch to system-assigned (spec §6)? Whichever is chosen gets Foundry User. | Founder | M6 | ANSWERED: system-assigned managed identity, per the spec's own §6 table. Do not use the accelerator's default user-assigned identity. |
| Q-005 | Verify the ACS callback JWT issuer, JWKS URL and audience before enabling `ACS_CALLBACK_JWT_AUDIENCE`. | Builder at M6 | M6 | ANSWERED: proceed as planned, verify at M6. |
| Q-006 | Run `az login` + `azd auth login` on the deploy machine. | Founder | M6 | ANSWERED: founder is completing this now (2026-09-27), in progress. |
| Q-007 | Should the bridge refuse to start when `ENABLE_WEB_CLIENT=true` and a telephony provider (e.g. ACS) is active at the same time, instead of only warning? Today (U05) it warns and starts. D-007 says the web client is "never set in a deployed environment," but nothing in code enforces that beyond the env var itself. See D-025. | Founder/partner | M6 (should be settled before deploy) | ANSWERED: refuse to start (see D-028). A live debug backdoor next to a real customer-facing line is not an acceptable risk for the pilot, even temporarily. Implemented and merged as U09 (PR #12). |
| Q-008 | `run_call_loop` (U06) awaits `handler.on_call_cap()` / `handler.on_idle()` with no timeout before the `finally` cancels the Voice Live task and before the caller releases the call slot (`cso` finding, PR #7). Today both hooks are no-ops so there is no live exposure, but once a telephony subclass gives them real I/O (a hangup call, a goodbye-message send), a hang there would leak the call slot and keep billing the Voice Live session. Should the unit that adds real hook bodies (or a follow-up) wrap the hook calls in `asyncio.wait_for` with a short timeout and add a hang/raise regression test? | Partner (sequence into the hook-implementing unit) | Whichever unit gives `on_call_cap`/`on_idle` real bodies | OPEN |
| Q-009 | `app/config_validator.py` (unmodified upstream code) accepts an API key as the sole credential for Voice Live, which conflicts with D-004's intent that agent-mode deployments authenticate via Entra ID only. `git diff upstream/main` on this file is empty, so it's out of scope to fix per D-016; today's agent-mode path (`VoiceLiveMediaHandler.connect_voicelive()`, U07) itself never uses the API key when `route` is set, so there is no live exposure yet. Should this upstream gap be closed (a small hook, per D-002) before M6 deploy, or is it acceptable as long as our own agent-mode path never takes the key-only branch? | Founder/partner | M6 (production security review) | OPEN — this becomes forced, not optional, once M6's identity switch lands: the same file's startup check (`validate_config()`, lines 32–39) exits the process unless *either* the API key *or* `AZURE_USER_ASSIGNED_IDENTITY_CLIENT_ID` is set, and a system-assigned identity has neither — see the M6 plan's C2 finding and its own **U-CFG** unit, which resolves this specific angle (the crash-loop, not the key-acceptance question) without setting an API key. Q-009's broader "should the upstream acceptance of a key be closed" question stays open for the production security review; U-CFG only makes sure M6's own deploy doesn't crash-loop or take the key-only path. |
| Q-010 | `CallSession._safe_hang_up()` (U08, PR #9) no longer treats a transient hang-up failure (5xx/429/network) as permanently terminal (D-027 fixed the retry-blocking bug), but nothing retries it *promptly* on the common path — the goodbye message plays, then hang-up gets a transient failure, and the only remaining retry today is `sweep()`'s `max_age`-gated `force_hang_up()`, so the caller could sit connected in silence for a while even though the call isn't permanently stuck. Separately, the underlying ACS `hang_up()` HTTP call itself has no timeout, so a hang there leaves `_hung_up` `True` for the rest of the session (a pre-existing gap, not a regression from U08). Opus code-review round 2 suggested one or two bounded retries with backoff inside `_safe_hang_up` as one option. Should a bounded retry-with-backoff be added now, or wait until M4's wiring units (U10–U12) make the real ACS retry/timeout characteristics concrete? | Partner (sequence into a wiring unit or a dedicated follow-up) | Call-ending latency under transient ACS failures; not blocking M3/M4 | OPEN |
| Q-011 | Can an ACS VoIP-only call (no PSTN number assigned yet) validate the deployed pipeline end-to-end, ahead of Q-002 resolving? The founder asked for this explicitly (2026-09-27) as a way to watch a live pipeline validation before the real number arrives. Needs verifying against the actual ACS `IncomingCall` event shape and Event Grid subscription behavior once M6 is deployed — don't assume it works from the spec alone. | Partner/builder, verify once M6 is deployed | M6 deploy plan should include how to test this; report back to founder with exact steps once confirmed | ANSWERED (analysis, revised after Opus review round 1; live confirmation still pending U15): yes, as a route-miss only — validates Event Grid → Container App → ACS answer → fallback → hang-up wiring end to end, but never reaches Voice Live/Foundry (a route-miss never opens media streaming), **and only once U10–U13 are merged and the ACS text-to-speech role (C3) and routing placeholder (C4) are actually fixed** — rev 1 of this answer understated both prerequisites. Verified by reading `routing.py`'s `called_number_from_event()` and the Task 8 plan's `bridge_calls.py` code/tests, which explicitly cover a non-phone-number `to` (an ACS communication-user identity) as a route-miss. The pass criterion is a `PlayCompleted` callback specifically, not `PlayFailed` (accepting either would mask a broken TTS role as a false pass). Exact steps (provision **two** test ACS user identities, issue a VoIP token, place the call via the ACS Calling SDK, watch bridge logs for `route=miss` then `PlayCompleted`) are written into the M6 plan's Q-011 section and become part of U15's DoD; the test is run by the builder/Cowork, not the founder himself, per HANDOFF.md §7. The actual live call is run and its outcome recorded when U15 executes, not in this planning session. |
| Q-012 | Must the deployed resource group be named exactly `rg-hireastra-voice-pilot`, or is the accelerator's own `rg-<environmentName>-<hash-suffix>` naming pattern acceptable (e.g. `rg-hireastra-voice-pilot-a1b2c`)? `main.bicep` computes the name as `rg-${environmentName}-${uniqueSuffix}` with no override parameter today. **Revised after Opus review round 1: this question's premise depends on Q-016 first** — if the resource group (or the ACS resource) already exists, this stops being a naming-preference question and becomes "reference the existing one." | Founder, informed by Cowork's inventory (Q-016) | U14a/U14d (a decision needed before `azd up` in U15, and now potentially before U14a's branch is even cut, per Q-016) | OPEN |
| Q-013 | ~~Does ACS/Event Grid support number-based filtering at all?~~ **Corrected after Opus review round 1: yes, it does** — Azure Event Grid's advanced-filter mechanism (e.g. `--advanced-filter data.to.PhoneNumber.Value StringIn <number>`) is a real, documented capability; the accelerator's `acs.postdeploy.ps1` simply never uses it. The real open question is only: should U16 apply that filter once the real number exists, given that doing so would also block any future VoIP-only smoke-test call from being delivered (a VoIP call's `to` has no `PhoneNumber.Value` to match)? | Founder/partner | U16's DoD only; nothing in M6/M7 | OPEN |
| Q-014 | ~~Is a Bicep-only fix needed to point App Insights at the existing resource?~~ **Corrected after Opus review round 1: no — this needs both a Bicep wiring fix and new Application Insights/OpenTelemetry SDK code in `server/`**, since `containerapp.bicep` declares an `appInsightsConnectionString` parameter that `main.bicep` never actually passes, and `server/pyproject.toml` has no Application Insights/OpenTelemetry dependency at all — even correct Bicep wiring alone would ship no telemetry. Does the founder want this real code+infra work done as part of M6, or is the M6 DoD's monitoring bullet explicitly rescoped to "Log Analytics container logs only, App Insights instrumentation deferred"? | **Founder/partner** (corrected — this is not the builder's call to make) | U14a's scope (whether it includes this), and transitively U15's monitoring verification step | OPEN |
| Q-015 | Now that U14b's circular-dependency finding (C1 in the M6 plan) is visible — the accelerator's own Bicep comment says a user-assigned identity exists specifically "to avoid circular dependency," and (per Opus review round 2) this covers **both** the registry pull **and** Key Vault secret resolution, not just the registry pull as first described — does the founder want to reconsider Q-004's system-assigned answer? Options: pure system-assigned with a two-phase `azd provision`/role-assignment/secret-attach/`azd deploy` sequence, or a hybrid (user-assigned kept for both the registry pull and Key Vault secrets; system-assigned only for role assignments applied after the app exists). | Founder | U14b (the unit can propose an option, but this is exactly the kind of architectural call D-014 flags for explicit sign-off, not a silent builder decision) | OPEN |
| Q-016 | Has Cowork already created `rg-hireastra-voice-pilot` and/or the ACS resource itself while working the Q-002 support ticket? If so, U14a/U14d's Bicep must reference them as `existing`, the same way it's being changed to reference `hireastra-resource`, rather than let Bicep create duplicates (a duplicate ACS resource in particular would mean the Event Grid subscription watches the wrong one). | Cowork, then founder/partner | U14a (do not cut U14a's branch until this inventory is back) | OPEN |
| Q-017 | Does the founder approve U14a–d's Bicep changes diverging from the pure upstream accelerator on `main.bicep`, `modules/aiservices.bicep`, `modules/identity.bicep`, `modules/containerapp.bicep`, `modules/roleassignments.bicep`, `modules/containerregistry.bicep`, `modules/acs.bicep`, `modules/keyvault.bicep`, `hooks/preprovision.ps1`, and `main.parameters.json` (file list corrected after Opus review round 2 — three files were missing) — accepting that a future `git merge upstream/main` may need manual conflict resolution on these files, in tension with D-002's "keep changes in as few files as possible so upstream merges stay clean" preference? Separately: should the existing (accelerator-default) Cognitive Services User/Reader role assignments be kept as-is or trimmed, now that the target resource is shared, cross-subscription and production-adjacent rather than a disposable per-environment resource? | Founder | U14a (a real D-002-tension question — get this answered before U14a's branch is cut, not discovered mid-review) | OPEN |
